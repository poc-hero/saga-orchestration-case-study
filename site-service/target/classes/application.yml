server:
  port: 8080
spring:
  application:
    name: site-service

site:
  services:
    uaa:
      url: http://localhost:8080

# Secret fourni par Vault Agent Injector (Phase 4 - Option A)
# En local : valeur par défaut vide ou via -Dapp.secrets.my-secret=xxx
app:
  secrets:
    my-secret: ${my-secret:}
